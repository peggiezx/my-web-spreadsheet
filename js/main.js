// main.js
document.addEventListener('DOMContentLoaded', () => {
    console.log('Web Spreadsheet is loaded!');
    // Later, you’ll write logic to create the grid, handle cell clicks, etc.
  });
  